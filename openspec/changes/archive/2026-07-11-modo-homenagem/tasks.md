## 1. Modelo de dados

- [x] 1.1 Adicionar `nomeHomenagem` (String) e getter/setter em `Carro.java`, além de `nomeOriginal` (String, análogo ao já existente em `Piloto`).
- [x] 1.2 Adicionar `nomeHomenagem` (String) e getter/setter em `Piloto.java` (campo já existe `nomeOriginal`, não precisa criar).
- [x] 1.3 Em `CarregadorRecursos.carregarListaCarrosArquivo`, ler o 11º campo posicional (`nomeHomenagem`) quando presente (`values.length > 10`), sem quebrar quando ausente.
- [x] 1.4 Em `CarregadorRecursos.carregarListaPilotos`, ler o 3º campo posicional (`nomeHomenagem`) quando presente, sem quebrar quando ausente.

## 2. Flag global e resolução de nome

- [x] 2.1 Adicionar `Global.MODO_HOMENAGEM` (boolean, default `true`) em `br/nnpe/Global.java`.
- [x] 2.2 Remover `Global.GERAR_IMAGEM_CIRCUITO_EM_MEMORIA`.
- [x] 2.3 Atualizar `DesenhoProceduralCircuito`, `CarregadorRecursos` (geração de imagem de fundo) e `LetsRace` (endpoint `/circuitoJpg/{nmCircuito}`) pra checar `Global.MODO_HOMENAGEM` em vez do flag removido.
- [x] 2.4 Em `CarregadorRecursos.carregarListaCarrosArquivo`, trocar `carro.setNome(Util.substVogais(name))` por: `carro.setNomeOriginal(name)` sempre, e `carro.setNome(MODO_HOMENAGEM && nomeHomenagem não vazio ? nomeHomenagem : name)`.
- [x] 2.5 Em `CarregadorRecursos.carregarListaPilotos`, trocar `piloto.setNome(Util.substVogais(name))` e `piloto.setNomeCarro(...)` pela mesma lógica condicional de 2.4. (Correção durante a implementação: `piloto.nomeCarro` era usado tanto pra parear Piloto↔Carro em `ligarPilotosCarros` quanto pra exibição — como `nomeHomenagem` não é mais uma função pura da chave, pareamento e exibição precisaram se separar: novo campo `Piloto.chaveCarro`, sempre a chave real, usado só pro pareamento em `ligarPilotosCarros` — `nomeCarro` continua sendo o nome de exibição.)
- [x] 2.6 Confirmar que `Util.substVogais` permanece intocado e em uso nos demais pontos (nomes de circuito) — não remover a função em si.

## 3. Resolução de imagem em modo homenagem

- [x] 3.1-3.3 (Descoberta durante a implementação: o "modelo colorido genérico" já existia — `CarregadorRecursos.pintarModeloV2` + `Global.FORCE_MODELO_V2` (default `true`), usado em `obterCapacete`/`obterCarroLado`/`obterCarroCimaSemAreofolio`/`obterCarroCima`, já implementava exatamente a cadeia modelo→sprite→arquivo→modelo pedida, só presa a um flag sem relação com homenagem. Trabalho real: os 4 `if (Global.FORCE_MODELO_V2)` viraram `if (Global.MODO_HOMENAGEM)`, e `FORCE_MODELO_V2` foi removido de `Global.java` — nenhum renderer novo foi necessário.)

## 4. Editor combinado de carros e pilotos

- [x] 4.1 Renomear `EditorCoresCarros` (classe e arquivo) pra `EditorCarrosPilotos`, atualizando todas as referências no código. (Também corrigido: `encontrarChave`, um hack que revertia `Util.substVogais` pra achar a chave real, foi removido — agora usa `Carro.getNomeOriginal()` diretamente, campo que não existia antes desta mudança.)
- [x] 4.2 Adicionar campo editável `nomeHomenagem` no card de cada carro (modo "Carros" existente).
- [x] 4.3 Adicionar um seletor de modo (Carros/Pilotos) no topo da janela, preservando a temporada selecionada ao trocar de modo.
- [x] 4.4 Implementar o modo "Pilotos": um card por piloto da temporada selecionada, mostrando imagem do carro/capacete daquele piloto (com as cores reais) somente leitura, com campos editáveis pra `nomeHomenagem` e `habilidade`.
- [x] 4.5 Persistir as edições do modo "Pilotos" em `pilotos.properties` (3º campo `nomeHomenagem`, 2º campo `Habilidade`), preservando `Carro` (1º campo) e comentários/ordem das linhas.
- [x] 4.6 Persistir as edições de `nomeHomenagem` do modo "Carros" em `carros.properties` (11º campo), preservando os demais 10 campos.
- [x] 4.7 Adicionar checkbox "Aplicar a todas as temporadas" junto ao campo `nomeHomenagem`, em ambos os modos (Carros/Pilotos), desmarcado por padrão.
- [x] 4.8 Implementar a busca de identidade pra propagação (refinado — ver design.md decisão 7): pra carro, o valor de `nomeHomenagem` que a linha tinha antes desta edição (lido do arquivo); pra piloto, chave real da properties — varrendo `carros.properties`/`pilotos.properties` de todas as temporadas sob `src/main/resources/properties/t*/`.
- [x] 4.9 Ao salvar com o checkbox marcado, atualizar só o campo `nomeHomenagem` de toda linha encontrada com a mesma identidade, em qualquer temporada, preservando os demais campos e a formatação de cada arquivo.
- [x] Validado: compila limpo (`mvn compile`), `mvn test` completo passa (564 testes), e a classe inicia sem exceção via `java -cp target/classes br.f1mane.editor.EditorCarrosPilotos` (smoke test de 8s). Verificação visual interativa (clicar nos botões, confirmar layout) fica pra validação manual do usuário — ver tarefa 7.8.
- [x] 4.10 Pitaco de design do usuário (ver design.md decisão 8): checkbox "Aplicar a todas as temporadas" e botão salvar lado a lado (ambos os modos); removido o botão/diálogo "Editar Cores" (redundante com os botões inline Cor 1/Cor 2); texto "Salvar" virou ícone 💾; classe renomeada de `EditorHomenagemCarrosPilotos` pra `EditorCarrosPilotos`. Recompilado, `mvn test` (570 testes) e smoke test de inicialização confirmados após a mudança.
- [x] 4.11 Bugs reportados pelo usuário (ver design.md decisão 9): (a) editor lia de classpath (`target/classes`) mas salvava em filesystem (`src/main/resources`), então reabrir sem rebuild mostrava o valor antigo — corrigido com `lerCarrosDoArquivo`/`lerPilotosDoArquivo` novos, lendo direto do filesystem, sem depender mais de `CarregadorRecursos.carregarListaCarros/carregarListaPilotos/ligarPilotosCarros`; (b) ícone de salvar (emoji) não renderizava — trocado por `IconeDisquete`, um `javax.swing.Icon` desenhado em `Graphics2D`; (c) modo Pilotos passa a mostrar só o capacete, vindo do sprite sheet real (`SpriteSheet.getCapacete`), não mais o modelo colorido, pra economizar espaço; (d) modo Carros ganhou uma linha extra mostrando o sprite original (lado/cima) ao lado do modelo pintado, pra comparação, com `extrairTime`/`indiceTime`/`indicePiloto` promovidos de `private` pra `public` em `CarregadorRecursos` (únicas mudanças de visibilidade necessárias); (e) a rolagem vertical pedida já existia (`JScrollPane`) e cresce automaticamente com o card maior — nenhuma mudança estrutural necessária. `mvn compile`, `mvn test` (570 testes) e smoke test de inicialização confirmados após as correções.
- [x] 4.12 Segunda passada de otimização de espaço a pedido do usuário (ver design.md decisão 10): removido o rótulo "Sprite original:", `lado`/`cima` passam a ocupar a largura toda da linha; sliders de Pot/Aero/Freios viram 3 `JSpinner` (100-999) numa linha só (`adicionarSlider` removido, `adicionarSpinner` novo — Freios ganha controle editável pela primeira vez); modo Pilotos: capacete+nome+carro na mesma linha (cabeçalho compacto), Homenagem+Habilidade na mesma linha. `CARD_H_CARRO` 284→266px, `CARD_H_PILOTO` 190→124px. `mvn compile`, `mvn test` (570 testes) e smoke test de inicialização confirmados.
- [x] 4.13 Bug reportado pelo usuário (ver design.md decisão 11): propagação de `nomeHomenagem` de carro parava de funcionar depois do 1º save sem o checkbox marcado, porque o valor "antes da edição" usado como chave de correspondência era relido do disco (já sobrescrito só na temporada editada) em vez de fixado na carga da temporada. Corrigido com `CarroEntry.nomeHomenagemCarregado`, capturado uma vez no load e só atualizado após propagação bem-sucedida; `lerNomeHomenagemAtual` (sem chamadores após a correção) removido; diálogo de salvamento agora confirma explicitamente quando a propagação ocorreu. Dados reais do usuário já presos nesse estado (Mercedes→Meredith, Williams→Wilton, ambos só em t2026) propagados manualmente pras 24 outras temporadas depois da correção. `mvn compile`, `mvn test` (570 testes) e smoke test de inicialização confirmados.

## 5. Preenchimento de nomeHomenagem — carros (todas as temporadas)

- [x] 5.1-5.6 Preenchido `nomeHomenagem` em `carros.properties` de todas as 26 temporadas (`t1968`...`t2026`). Descoberta ao inspecionar todos os arquivos: 17 temporadas já usam nome de imagem canônico de fábrica (`ferrari.png`, `mclaren.png`, etc.) — só extensão removida; 9 temporadas (`t1968`, `t1972`, `t1974`, `t1987`, `t1988`, `t1990`, `t1993`, `t2007`, `t2008`) usam convenção antiga com prefixo/sufixo (`tn_ANOvoi-equipemodelo.png`, `Equipe_MODELO.png`, `Equipe-MODELO.png`) — essas 95 linhas foram mapeadas manualmente pro nome da equipe (ex.: `tn_1972voi-lotus72d.png`→`lotus`, `Williams_FW11B.png`→`williams`). Nota: `redbull.png` (algumas temporadas) e `rbr.png` (outras) canonicalizam pra nomes diferentes mesmo sendo a mesma equipe real — aceito como consequência de derivar do nome do arquivo, não de conhecimento histórico de marca; editável depois pelo usuário/editor se quiser unificar.

## 6. Preenchimento de nomeHomenagem — pilotos (todas as temporadas)

- [x] 6.1-6.6 Preenchido `nomeHomenagem` em `pilotos.properties` de todas as 26 temporadas. O jogo tem só 199 pilotos reais distintos (mesmo piloto repete a mesma chave em várias temporadas) — pesquisada a nacionalidade de cada um e escolhido um sobrenome comum da mesma nacionalidade, preservando a inicial do primeiro nome (ex.: `A.Senna` brasileiro → `A.Silva`; `F.Alonso` espanhol → `F.Almodovar`). Validado ao final: 556 linhas em 26 arquivos, contagem de linhas e conjunto de chaves idênticos ao `git HEAD` em todo arquivo, todo campo `nomeHomenagem` não vazio.
  - **3 pilotos com nacionalidade incerta** (nomes obscuros, não identificados com confiança na pesquisa) — sinalizar pro usuário revisar/corrigir via o editor: `G.Garde` (chute: escandinavo), `P.Reverson` (chute: francês), `T.Belso` (chute: húngaro).
  - Corrigidos durante o preenchimento: 3 chaves reais tinham variantes com erro de digitação já existentes no jogo (`G.Russel`/`G.Russell`, `K.Kobaiashi`/`K.Kobayashi`, `K.Raikkinen`/`K.Raikkonen`) e uma com espaço à direita (`"U.Katayama "`) — cada variante recebeu o mesmo nome-homenagem do piloto real correspondente, sem tentar corrigir a chave original (fora do escopo desta mudança).

## 7. Testes e validação

- [x] 7.1 Teste unitário: `Carro`/`Piloto` carregados com `nomeHomenagem` presente e `MODO_HOMENAGEM=true` expõem o nome-homenagem via `getNome()`. (`CarregadorRecursosModoHomenagemTest.modoHomenagemAtivo_usaNomeHomenagemDoCarroEDoPiloto`, com fixtures sintéticos em `src/test/resources/properties/tcomhomenagem/`.)
- [x] 7.2 Teste unitário: `MODO_HOMENAGEM=false` expõe o nome real (nomeOriginal/chave), sem `substVogais`. (`modoHomenagemInativo_usaNomeRealSemSubstVogais`.)
- [x] 7.3 Teste unitário: `carros.properties`/`pilotos.properties` sem o campo `nomeHomenagem` continuam carregando sem exceção, com fallback pro nome real. (`temporadaSemNomeHomenagem_carregaSemQuebrar_comFallbackParaNomeReal`, fixtures em `tsemhomenagem/`.) Cobertura extra não prevista originalmente: `ligarPilotosCarros_pareiaPeloNomeOriginal_naoPeloNomeExibido`, garantindo que o pareamento piloto↔carro (que dependia implicitamente de `substVogais` produzir o mesmo valor nos dois lados) continua funcionando agora que o nome exibido pode divergir da chave real.
- [x] 7.4 Teste unitário: nenhuma referência restante a `Global.GERAR_IMAGEM_CIRCUITO_EM_MEMORIA` no código-fonte. (`globalGerarImagemCircuitoEmMemoria_naoExisteMais`; adicionado também `globalForceModeloV2_naoExisteMais`, já que esse flag foi removido junto — ver design.md decisão 5.)
- [x] 7.5 Teste unitário/manual: propagar `nomeHomenagem` de um carro com o checkbox marcado atualiza todas as temporadas com o mesmo nome canônico de imagem, sem afetar outras equipes nem outros campos. (Não automatizado — lógica de propagação vive em métodos privados de `EditorCarrosPilotos`, fortemente acoplados a Swing/I/O de arquivo. Validado manualmente via reflection, invocando `propagarNomeHomenagemCarro` real da classe compilada contra os `.properties` reais do repositório — achou e confirmou o bug da decisão 11 antes da correção, e confirmou a propagação completa e correta pra 12/24 temporadas depois dela.)
- [ ] 7.6 Teste unitário/manual: propagar `nomeHomenagem` de um piloto com o checkbox marcado atualiza todas as temporadas em que a mesma chave real aparece, sem afetar outros pilotos nem outros campos. (Mesma observação de 7.5 — validação manual pendente.)
- [x] 7.7 Rodar `mvn test` completo pra garantir que nenhum teste existente (incluindo os que já cobrem `objetos-cenario-circuito`/geração procedural de circuito) quebrou. (570 testes, 0 falhas, 0 erros, 2 skipped — 564 preexistentes + 6 novos.)
- [ ] 7.8 Validação manual: abrir `EditorCarrosPilotos`, alternar entre modo Carros/Pilotos numa mesma temporada, editar um `nomeHomenagem` de cada tipo (com e sem o checkbox de propagação) e confirmar a gravação via `git diff` nos `.properties`. (Compilação, `mvn test` completo e smoke test de inicialização via `java -cp target/classes ...` sem exceção já confirmados; interação visual/clique fica pro usuário.)
- [ ] 7.9 Validação manual: rodar uma simulação/corrida com `MODO_HOMENAGEM=true` e depois `false`, conferindo visualmente a troca de nomes exibidos e da imagem de fundo do circuito (procedural vs. arquivo real).
