## 1. Cores de box e zebra (circuito-cores-box-zebra)

- [x] 1.1 Adicionar campos `corBox1`, `corBox2`, `corZebra1`, `corZebra2` (`Color`, transientes, `@JsonIgnore`) em `Circuito.java`, junto a `corFundo`/`corAsfalto`, com getters/setters simples.
- [x] 1.2 Adicionar quatro indicadores de cor clicáveis no editor (`MainPanelEditor`), reaproveitando `criaIndicadorDeCor()`/`atualizaCorLabel()`, posicionados junto aos indicadores de "Cor de Fundo"/"Cor do Asfalto".
- [x] 1.3 Refletir os valores de `corBox1`/`corBox2`/`corZebra1`/`corZebra2` do circuito carregado nos indicadores ao abrir/trocar de circuito no editor.
- [x] 1.4 Atualizar `PainelCircuito.desenhaTintaPistaZebra()` para usar `corZebra1`/`corZebra2` do circuito quando ambas não forem `null`, com fallback para `Color.WHITE`/`Color.RED`. Também corrigido o método irmão `DesenhoProceduralCircuito.desenhaTintaPistaEZebra()` (usado pelo preview do editor e pela geração de imagem em memória), que tinha o mesmo hardcode.
- [x] 1.5 Atualizar o desenho da área de box (editor e geração de imagem em memória, em `DesenhoProceduralCircuito.desenhaVagasBox()`) para usar `corBox1`/`corBox2` como cor padrão de slot quando não há carro/time associado ao contexto, sem alterar o comportamento em corrida real (`PainelCircuito.desenhaBoxes()` continua priorizando `carro.getCor1()`/`getCor2()`, não foi tocado).
- [x] 1.6 Testes: `CircuitoCoresBoxZebraTest` (defaults nulos, XML existente carrega sem erro, round-trip XMLEncoder/XMLDecoder) e `DesenhoProceduralCircuitoTest` (zebra usa cores customizadas/fallback branco-vermelho/não mistura customizada com padrão; box usa cores customizadas/fallback ciano-magenta). 11 testes novos, todos passando.

## 2. Combobox de seleção de circuito e remoção do botão de créditos (dev-editor-tools)

- [x] 2.1 Substituir `lblCircuitoAtual` por um `JComboBox<CircuitoComboItem>` populado a partir da mesma lista usada por `circuitosXml`/`popularCircuitos()`, exibindo o nome amigável de `circuitos.properties` (`repopularComboCircuitos()`) como texto (`toString()`) e o arquivo XML como valor interno.
- [x] 2.2 Selecionar um item no combobox carrega o circuito correspondente (`comboCircuito` ActionListener resolve o `arquivoXml` do item e chama `carregarCircuitoExistente`, o mesmo caminho usado por Anterior/Próximo).
- [x] 2.3 Anterior/Próximo (e qualquer carregamento de circuito) atualizam a seleção do combobox via `atualizarBotoesNavegacao()`, agora também chamado ao final de `carregarCircuitoExistente()`; guarda `sincronizandoComboCircuito` evita disparar um segundo carregamento.
- [x] 2.4 Fallback: quando `circuitos.properties` não pode ser lido, `nomesAmigaveisCircuitos` é preenchido com o próprio nome do arquivo (mesma lista usada para montar o combobox).
- [x] 2.5 Removidos o botão "Créditos", o método `creditos()`, o campo `creditos` (boolean) e o branch do mouse listener associado em `MainPanelEditor`; `Circuito.creditos` (Point) e seu getter/setter mantidos apenas para compatibilidade de leitura de XMLs antigos (a marcação visual passiva do ponto, se já definido em um circuito legado, continua sendo desenhada — só o fluxo de edição foi removido).
- [x] 2.6 Verificação manual (não há display interativo neste ambiente de execução para testar Swing diretamente, mesmo padrão já estabelecido em `CircuitoObjetosCenarioIntegrationTest`): compilação limpa confirma que não sobrou nenhuma referência a `lblCircuitoAtual`/`creditos`. Pendente validar visualmente: navegação Anterior/Próximo mantém o combobox sincronizado; selecionar um item no combobox carrega o circuito certo; nenhum botão de créditos aparece na janela do editor.

## 3. Modelo de vértices com haste de curvatura (objeto-livre-vetorial)

- [x] 3.1 Criada classe bean `PontoCurva` (construtor sem args + `PontoCurva(Point)`, getters/setters `posicao`/`hasteFim`, mais `getControleSaida()`/`getControleEntrada()` encapsulando a regra de degeneração para reta) compatível com `XMLEncoder`/`XMLDecoder`.
- [x] 3.2 Adicionado campo `List<PontoCurva> vertices` em `ObjetoLivre`; campo legado `pontos` (`List<Point>`) mantido intacto (getter/setter) apenas para leitura/compatibilidade.
- [x] 3.3 Implementada via `obterVerticesEfetivos()` (sintetiza a partir de `pontos` sem mutar estado, usado por `gerar()`) e `inicializarVerticesSeNecessario()` (migra `pontos`→`vertices` de fato, chamado pelo editor ao iniciar uma edição — group 4).
- [x] 3.4 `ObjetoLivre.gerar()` reescrito: monta um `GeneralPath` fechado usando `path.curveTo(...)` por segmento (cúbica de Bézier equivalente a `CubicCurve2D`, controle de saída do vértice de origem e controle de entrada espelhado do vértice de destino); com hastes neutras a cúbica degenera matematicamente numa reta exata (não apenas visualmente).
- [x] 3.5 `ObjetoLivre.desenha()`/`obterArea()` atualizados para usar `generalPath` no lugar do `Polygon` removido; novo getter público `getForma()` para inspeção (editor e testes).
- [x] 3.6 Testes em `PontoCurvaTest` (5) e `ObjetoLivreTest` (7): haste nula produz segmento reto idêntico ao polígono antigo (`contains`/`bounds`); haste ajustada produz curva que sai do retângulo do polígono reto (provado matematicamente via o mínimo da cúbica, não só observado); `inicializarVerticesSeNecessario` migra/preserva; round-trip XMLEncoder/XMLDecoder de um objeto legado (só `pontos`) desenha forma idêntica. Duplicação de `ObjetoLivre` no editor (`MainPanelEditor`) também passou a copiar `vertices` (não só `pontos`), senão duplicar um objeto curvo perderia a curva — achado ao revisar o fluxo existente, corrigido no mesmo commit.

## 4. Edição de pontos e hastes no editor (objeto-livre-vetorial)

- [x] 4.1 Modo de edição de pontos ligado/desligado pelo botão "Editar Pontos"/"Parar de Editar Pontos" no painel de ajuste rápido (menu de contexto, botão direito sobre o objeto) quando o alvo é `ObjetoLivre` — `iniciarEdicaoPontosObjetoLivre`/`encerrarEdicaoPontosObjetoLivre`. Marcadores desenhados por `desenhaMarcadoresEdicaoPontos`: círculo amarelo em cada vértice; quando a haste está ajustada, uma linha ciano até um círculo azul na ponta da haste.
- [x] 4.2 Arraste de vértice (clique **esquerdo** no marcador) implementado em `mousePressed`/`mouseDragged`/`mouseReleased`: atualiza `posicao` do `PontoCurva`; se o vértice já tinha haste ajustada, ela se move junto (delta rígido) para não distorcer a curva ao mover o ponto.
- [x] 4.3 Arraste de haste (clique **direito** no marcador do vértice, ou na própria ponta da haste se já ajustada) atualiza `hasteFim`; `gerar()` é chamado a cada passo do arraste para recalcular a curva em tempo real.
- [x] 4.4 O novo branch de arraste é verificado em `mousePressed` antes de qualquer outra lógica, mas só reivindica o clique (`return true`) quando de fato acerta um marcador dentro do raio de tolerância (`RAIO_MARCADOR_EDICAO_PONTOS_PX`); caso contrário cai para o fluxo normal (seleção/arraste de objeto inteiro, colocação de nó), preservando o comportamento existente quando o modo não está ativo ou o clique não acerta nenhum marcador.
- [x] 4.5 Verificação manual (mesma limitação de ambiente sem display interativo, já documentada em 2.6): compilação limpa e suíte completa (185/185) confirmam que a integração não quebrou nada. Pendente validar visualmente: arrastar vértice reposiciona e redesenha; arrastar haste curva os dois segmentos adjacentes; alternar "Editar Pontos" não deixa o canvas travado nem interfere no arraste normal de outros objetos.
- [x] **Achado durante a implementação (fora da lista original, necessário para o Grupo 4 ser alcançável)**: `ObjetoLivre` nunca esteve registrado em `TipoObjetoPista` (não aparecia no combo "Criar Objeto") e o clique-para-adicionar-ponto na criação só tratava `ObjetoTransparencia`, não `ObjetoLivre` — ou seja, não havia como sequer criar um `ObjetoLivre` pela UI antes desta mudança. Corrigido: `ObjetoLivre` registrado em `TipoObjetoPista` (`LIVRE`), fluxo de clique estendido para `ObjetoLivre` (mesmo padrão do `ObjetoTransparencia`, sem a transparência de 125 específica de máscara de box), e `ObjetoLivre` ganhou um construtor com cor primária/secundária/transparência padrão (mesmo padrão de `ObjetoArquibancada` etc.) para não desenhar com `NullPointerException` assim que criado.

## 5. Tipo e padrões de preenchimento do ObjetoLivre (objeto-livre-vetorial)

- [x] 5.1 Criado enum `TipoObjetoLivre` (`POLIGONO_SIMPLES`, `VEGETACAO_DENSA`, `AGUA`, `BRITA`) e campo `tipo` em `ObjetoLivre`, com `POLIGONO_SIMPLES` como padrão do construtor sem args (e fallback em `setTipo(null)`).
- [x] 5.2 Adicionada seleção de `tipo` (`tipoObjetoLivreCombo`) em `FormularioObjetos`, com os rótulos das cores herdadas (`labelLegendaCor1`/`labelLegendaCor2`, agora campos em vez de `JLabel` locais) trocando dinamicamente para "Cor de Fundo"/"Cor de Padrão" em `carregarCampos()` quando o objeto é `ObjetoLivre`, e voltando para "Cor Primária"/"Cor Secundária" para os demais tipos.
- [x] 5.3 `POLIGONO_SIMPLES` preserva o preenchimento sólido de `corPimaria` (comportamento idêntico a antes desta mudança, sem uso de `corSecundaria`).
- [x] 5.4-5.6 Implementado `desenhaPadrao()`/`desenhaPrimitivaPadrao()` em `ObjetoLivre`: clip na forma final (já rotacionada/escalada), grade determinística de passo `Carro.ALTURA * zoom` (linhas alternadas com meio-passo de deslocamento), com `corSecundaria`: cruz de traços curtos (vegetação densa), arco raso (água), círculo preenchido (brita).
- [x] 5.7 Testes em `ObjetoLivreTipoPadraoTest` (7): tipo padrão e fallback de `null`; `POLIGONO_SIMPLES` não usa `corSecundaria`; os três tipos novos desenham sem exceção e usam `corSecundaria` visivelmente dentro da forma; determinismo comprovado por igualdade exata de todos os pixels (`DataBufferInt`) entre duas renderizações sucessivas.

## 6. Validação final

- [x] 6.1 Coberto por automação equivalente (sem display interativo neste ambiente): `CircuitoCoresBoxZebraTest`/`DesenhoProceduralCircuitoTest` provam fallback idêntico ao anterior para zebra/box sem cores definidas; `ObjetoLivreTest`/`ObjetoLivreCircuitoIntegrationTest` provam que um `ObjetoLivre` legado (só `pontos`) desenha forma idêntica à anterior após round-trip XML real. Pendente validação visual humana no editor.
- [x] 6.2 Suíte completa: **194/194 testes passando** (`mvn test`), incluindo os 34 testes novos desta mudança. `mvn clean package -Ph2` também concluído com sucesso (jar gerado).
- [x] 6.3 Coberto por `ObjetoLivreCircuitoIntegrationTest.objetoLivreCurvoComTipo_sobrevivePersistenciaCompletaDoCircuito`: cria um `ObjetoLivre` com haste ajustada (curva) e `tipo=AGUA`, adiciona a um circuito, persiste e recarrega via `XMLEncoder`/`XMLDecoder` (o mesmo mecanismo usado por "Salvar Pista Atual"/navegação no editor), confirma que `tipo`, vértices e a curvatura da haste sobrevivem, e que a forma recarregada ainda produz a curva (mesma prova matemática de bounds negativo). Pendente validação manual do fluxo de arraste em si na UI (não automatizável sem display).
