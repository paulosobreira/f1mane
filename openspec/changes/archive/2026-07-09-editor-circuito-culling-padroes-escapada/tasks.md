## 1. Culling por viewport no editor

- [x] 1.1 Em `MainPanelEditor.java`, criar um helper `estaVisivelNoViewport(ObjetoPista)` que calcula o retângulo de teste (`obterArea()` quando `angulo==0`; retângulo centrado no centro de `obterArea()` com metade do lado igual a `Math.hypot(largura, altura)/2` quando `angulo!=0`) e testa interseção com `limitesViewPort()`.
- [x] 1.2 Chamar `estaVisivelNoViewport(...)` em `desenhaObjetosNivel` antes de `objetoPista.desenha(g2d, zoom)`, pulando a chamada quando `false`, sem alterar a ordem de iteração nem os demais filtros já existentes (nível, `desenhaObjetosDesenho`).
- [x] 1.3 Confirmar que overlays de preview/edição (objeto em criação, marcadores de edição de pontos, contorno de seleção) continuam desenhados fora de `desenhaObjetosNivel`, sem passar pelo novo corte.
- [x] 1.4 Testar manualmente com um circuito de muitos objetos: pan pelo editor e confirmar que objetos aparecem/desaparecem corretamente ao entrar/sair do viewport, sem "sumiço" de objetos parcialmente visíveis. **(testado manualmente pelo usuário, confirmado OK)**

## 2. Rotação do padrão e novos tipos de ObjetoLivre

- [x] 2.1 Em `ObjetoLivre.java`, extrair a construção de `formaZoomLocal` (path traduzido a `posicaoQuina` e escalado por `zoom`, sem rotação) reaproveitando o cálculo já existente antes da rotação em `desenha()`.
- [x] 2.2 Alterar `desenhaComClipSemAntialiasing` para aplicar `g2d.rotate(...)` (rotação em torno do pivô equivalente ao centro de `formaFinal` em espaço de tela) antes do clip, clipar usando `formaZoomLocal` (não `formaFinal`), e restaurar o transform original do `Graphics2D` no `finally`, junto do clip/stroke/antialiasing já restaurados.
- [x] 2.3 Atualizar `desenhaPadraoEmGrade`, `desenhaPadraoVegetacao` e `desenhaBrita` para usar `formaZoomLocal.getBounds()` como base da grade em vez de `formaFinal.getBounds()`.
- [x] 2.4 Adicionar `LISTRADO` e `XADREZ` a `TipoObjetoLivre.java`.
- [x] 2.5 Implementar `desenhaPadraoListrado` (listras retas paralelas ao eixo X local, largura baseada em `PASSO_PADRAO_LOCAL * zoom`) e `desenhaPadraoXadrez` (grade de células quadradas, preenchendo quando `(coluna+linha)%2==0`), ambos roteados por `desenhaComClipSemAntialiasing` e usando `formaZoomLocal.getBounds()`.
- [x] 2.6 Atualizar o dispatch de `tipo` em `ObjetoLivre.desenha()` para chamar os dois novos métodos.
- [x] 2.7 Atualizar `FormularioObjetos.java` (`tipoObjetoLivreCombo`) para listar `LISTRADO` e `XADREZ` como opções selecionáveis. **(já automático: o combo usa `TipoObjetoLivre.values()` diretamente, sem lista hardcoded nem i18n de labels para este enum)**
- [x] 2.9 **(revisado após feedback)** Corrigido para que `angulo` rotacione só o conteúdo do padrão, nunca a silhueta do objeto livre: `desenhaComClipSemAntialiasing` agora clipa ANTES de rotacionar o transform (clip fica fixo em espaço de dispositivo, conteúdo desenhado depois sai rotacionado mas recortado pela silhueta fixa); `ObjetoLivre.obterAreaClique()` sobrescrito para não rotacionar (a área de clique precisa coincidir com a forma fixa realmente desenhada); `TOLERANCIA_CLIQUE_PX` promovida de `private` para `protected` em `ObjetoPista` para reuso. Coberto por `ObjetoLivrePadraoRotacaoTest.angulo_naoAlteraASilhuetaDoObjetoLivre` (retângulo não quadrado: bounds da cor primária idênticos com `angulo=0` e `angulo=90`).
- [x] 2.10 **(revisado após feedback)** Corrigido para que o padrão rotacionado cubra TODA a área do objeto, não só a área do bounding box original: novo helper `areaCoberturaPadrao(Shape)` retorna um quadrado concêntrico com metade do lado igual ao raio circunscrito do bounding box (mesmo cálculo do raio circunscrito do culling de viewport, item 1.1), usado pelos 5 métodos de padrão como base dos laços em vez de `formaLocal.getBounds()` direto — garante cobertura total mesmo depois de rotacionado, já que o clip (a silhueta real) recorta o excesso. Verificado revertendo temporariamente `desenhaBrita` para o bounds antigo e confirmando que o teste abaixo falha (canto sem brita), depois restaurando a correção. Coberto por `ObjetoLivrePadraoRotacaoTest.britaComRotacao_cobreTodaAAreaIncluindoOsCantos` (retângulo 200x50 a 45°: os 4 cantos continuam com pontos de brita).
- [x] 2.8 Testar manualmente no editor: criar um `ObjetoLivre` com cada tipo de padrão não sólido, girar o ângulo e confirmar visualmente que o padrão gira dentro da forma, e que a forma em si NÃO gira. **(testado manualmente pelo usuário, confirmado OK)**

## 3. Vão entre módulos do CAMINHAO

- [x] 3.1 Em `ObjetoConstrucao.java`, adicionar a constante `VAO_MODULOS_CAMINHAO = MARGEM_INTERNA / 2`.
- [x] 3.2 Substituir os usos de `MARGEM_INTERNA` por `VAO_MODULOS_CAMINHAO` em `larguraEfetiva()` e `desenhaCaminhao()` (posição/largura do vão entre cabine e carroceria), mantendo `MARGEM_INTERNA` inalterado para a margem de borda aninhada.
- [x] 3.3 Testar manualmente: criar um `ObjetoConstrucao` do tipo `CAMINHAO` no editor e confirmar visualmente que o vão entre os dois módulos ficou menor, sem alterar a espessura da borda de cada módulo. **(testado manualmente pelo usuário, confirmado OK)**

## 4. Novo modelo de ObjetoEscapada (saída/retorno ancorados ao traçado)

- [x] 4.1 Reescrever `ObjetoEscapada.java`: remover os campos/desenho de elipse (`largura`/`altura` como comprimento/amplitude, `getExterno()`, `centro()`), adicionar `pontoSaida`, `pontoRetorno` e `tracadoOrigem` (`Point`/`int`, seguindo o modelo de `ObjetoGuardRails`), e um `gerar()` que constrói um `GeneralPath` do trajeto entre os dois pontos.
- [x] 4.2 Adicionar em `MainPanelEditor.java` (`noMaisProximoTracado1e2`/`noMaisProximoDoTracado`) um helper de busca do nó mais próximo restrito a uma lista de traçados (`circuito.getPista1Full()`/`getPista2Full()`), retornando o `No`, para reaproveitar tanto na saída (ambos traçados) quanto no retorno (só `tracadoOrigem`).
- [x] 4.3 Definir a constante `TOLERANCIA_ESCAPADA_PX` (2px de tela, conforme pedido) e implementar a validação de saída: clique esquerdo aceito só se o nó mais próximo entre `pista1Full`/`pista2Full` estiver dentro da tolerância; caso contrário, mostrar `JOptionPane` de erro via `Lang.msg("pontoEscapadaInvalido")` e não posicionar o ponto.
- [x] 4.4 Implementar a validação de retorno: clique direito aceito só se o nó mais próximo do MESMO traçado de `tracadoOrigem` estiver dentro da tolerância; caso contrário, mostrar o mesmo alerta e manter o objeto em modo de criação (não finalizar).
- [x] 4.5 Ao aceitar o retorno, finalizar a criação (adicionar a `circuito.getObjetos()`, chamar `gerar()`, limpar estado de criação), seguindo o mesmo fluxo de finalização já usado para `ObjetoGuardRails`/`ObjetoTransparencia` em `clickEditarObjetos`.
- [x] 4.6 Adicionar preview visual do objeto em criação (marcador no `pontoSaida`, se já definido) via `desenhaPreObjetoEscapada`, análogo a `desenhaPreObjetoGuardRails`.
- [x] 4.7 Estender o modo de edição de pontos (`editandoPontosEscapadaDe`, mecanismo próprio análogo a `editandoPontosGuardRailsDe`) para permitir arrastar `pontoSaida`/`pontoRetorno` de um `ObjetoEscapada` já criado, reaplicando a mesma validação de tolerância e traçado ao soltar (`finalizarArrastePontoEscapada`), revertendo para a posição anterior e alertando quando inválido.
- [x] 4.8 Atualizar `FormularioObjetos.java` (formulário modal) e `criaPainelAjusteRapido` (menu de contexto) para remover altura/ângulo do modelo antigo e expor Largura (espessura do traçado) + cores + botão "Editar Pontos", no mesmo espírito de GuardRails.
- [x] 4.9 Testar manualmente no editor: criar um `ObjetoEscapada` clicando perto do traçado 1, tentar finalizar perto do traçado 2 (deve alertar e não finalizar), depois finalizar corretamente perto do traçado 1; testar também clique de saída longe de qualquer traçado (deve alertar). **(testado manualmente pelo usuário, confirmado OK — também validado por integração via `MouseEvent` reais em `MainPanelEditorEscapadaCliqueTest`)**
- [x] 4.10 **(revisado após feedback do usuário — bug real)** Corrigido o modelo para ser um encadeamento de N pontos (`List<Point> pontos`, como `ObjetoGuardRails`), não 2 pontos fixos: só o primeiro clique esquerdo (entrada) e o clique direito que finaliza (saída) são validados; cliques esquerdos intermediários são livres, acrescentados sem nenhuma validação. O bug relatado ("valida no segundo nó, não no nó de entrada") vinha do modelo antigo de 2 pontos, onde um segundo clique esquerdo (pretendido como ponto livre) reescrevia e revalidava a "entrada". `desenhaPreObjetoEscapada` e o modo de edição de pontos (`tentarIniciarArrastePontoEscapada`/`finalizarArrastePontoEscapada`/`desenhaMarcadoresEdicaoPontosEscapada`) foram reescritos para N pontos, com a regra de validação restrita ao primeiro/último índice. Coberto por `MainPanelEditorEscapadaCliqueTest` (clique real via `MouseEvent`: entrada validada, ponto livre no meio aceito sem validação, saída validada e finaliza).
- [x] 4.11 **(adicionado após feedback do usuário)** `ObjetoEscapada` ganha `indiceEntrada`/`indiceSaida` (`No.getIndex()` do nó ancorado), gravados pelo editor na validação de criação e de edição (drag). `TestePista.java` foi reconectado ao novo modelo: `obterTracadoEscapadaAtivo` passa a procurar em `circuito.getObjetos()` um `ObjetoEscapada` cujo `[indiceEntrada, indiceSaida]` cubra o índice atual do carro de teste (em vez do extinto `circuito.getEscapeMap()`), e `construirTracadoEscapada`/`pontoNoTrajeto` interpolam por comprimento de arco ao longo de `escapada.getPontos()`. Coberto por `TestePistaEscapadaTest` (dentro/fora da zona, com/sem modo escapada, objeto degenerado sem índices) e `MainPanelEditorEscapadaRevetorizarTest` (revetorizar com os mesmos parâmetros, como o botão "Teste Pista" sempre faz, não invalida os índices gravados).
- [x] 4.12 **(bug real encontrado e corrigido após feedback do usuário: "só funciona depois de recarregar")** `calculaOffsetTelaEscapada` calculava o deslocamento local↔tela a partir de `posicaoQuina`, mas `posicaoQuina` nunca era resincronizado com os novos bounds de `pontos` depois de mover um ponto — a partir da segunda leitura do deslocamento (mesmo arrasto ou arrasto seguinte), o valor calculado ficava errado, corrompendo a posição gravada do ponto (o índice de nó continuava certo, só a posição visual/de trajeto ficava deslocada). Corrigido chamando `setPosicaoQuina(obterArea().getLocation())` depois de todo `gerar()` disparado por edição de ponto (em `mouseDragged` e nos dois ramos de `finalizarArrastePontoEscapada`). Coberto por `MainPanelEditorEscapadaArrastarPontoTest` (arrastar entrada, arrastar saída, e confirmar que o Teste Pista reconhece a mudança imediatamente, sem reload). `ObjetoGuardRails` tem o mesmo padrão de código e provavelmente o mesmo bug — sinalizado como tarefa separada (spawn_task), não corrigido aqui.

## 5. Remoção do modelo antigo de escapada

- [x] 5.1 **(revisado durante a implementação — ver nota abaixo)** `Circuito.java`: `gerarEscapeMap()`/`gerarEscapeList()` tiveram o corpo que lia a API antiga de `ObjetoEscapada` removido, mas os campos `pista4Full`/`pista5Full`/`escapeMap` e seus getters foram **mantidos** (não removidos), porque `Piloto.java` e `PainelCircuito.java` acessam `getPista4Full()`/`getPista5Full()` diretamente por índice (`.get(noAtual.getIndex())`) — removê-los quebraria essa leitura fora da classe Escapada, violando "não alterar a implementação de escapada na classe piloto". `preencheTracadoEscapeSuave()` (agora sem chamador) foi removida por estar morta.
- [x] 5.2 `Circuito.getEscapeMap()`/`getPista4Full()`/`getPista5Full()` continuam existindo e agora sempre retornam, respectivamente, um mapa vazio e listas do mesmo tamanho de `pistaFull`/`pista1Full`/`pista2Full` com todas as posições `null` — mesmo formato que já ocorria para qualquer circuito sem `ObjetoEscapada`, então `Piloto.java` continua compilando e executando sem alteração e sem exceção.
- [x] 5.3 **(revisado)** `PontoEscape.java` foi **mantido** (não removido): é o tipo de chave de `Circuito.getEscapeMap()`, lido diretamente por `Piloto.java`, `PainelCircuito.java`, `ControleRecursos.java`, `MainPanelEditor.java` e `TestePista.java` — removê-lo quebraria a compilação desses 5 arquivos fora do escopo desta mudança.
- [x] 5.4 Removida a chamada a `reprocessarEscapadas()`/`reprocessaEscapadaSeNecessario` do fluxo de largura/altura/ângulo do modelo antigo (esses campos deixaram de ser exibidos para Escapada em `criaPainelAjusteRapido`/`FormularioObjetos`); as chamadas remanescentes (drag do objeto inteiro, finalização de criação) foram deixadas como estão por serem agora no-ops baratos e inofensivos (`reprocessarEscapadas()` só repopula listas vazias), evitando alterações de maior risco sem benefício comportamental.
- [x] 5.5 Escrito `scratchpad/remove_escapada.py`, que localiza cada bloco `<void method="add"><object class="br.f1mane.entidades.ObjetoEscapada">...</object></void>` por casamento balanceado de tags, verifica que nenhum `id` definido dentro do bloco é referenciado (`idref`) fora dele (nenhum conflito encontrado nos 33 arquivos) e remove os blocos.
- [x] 5.6 Rodado o script: 138 blocos removidos em 33 arquivos, 0 conflitos; `grep -rl ObjetoEscapada src/main/resources/circuitos/*.xml` retorna vazio; todos os 33 XMLs continuam bem formados (validado com `xml.etree.ElementTree`).
- [x] 5.7 `mvn clean package -Ph2 -DskipTests` completa sem erro; validado além disso com `java -cp target/flmane.jar br.f1mane.MainFrameSimulacao 2024 Catalunya 3` (circuito afetado pela remoção, exemplo do próprio CLAUDE.md) — corrida completa as 3 voltas sem exceção de carregamento de XML.

## 6. Testes automatizados

- [x] 6.1 `CircuitoEscapeMapTest.java` removido (as 3 asserções cobriam exclusivamente a geração de onda que não existe mais). `TestePistaEscapadaTest.java` reescrito para cobrir o novo modelo reconectado (ver 4.11): dentro/fora do intervalo `[indiceEntrada, indiceSaida]`, com/sem modo escapada, e um `ObjetoEscapada` degenerado (índices no default `-1`) que não deve travar nem ativar nada.
- [x] 6.2 Revisados os 7 arquivos listados: `DesenhoProceduralCircuitoTest`, `ObjetoPistaNivelDesenhoTest`, `ObjetoDesenhoLimitesTest`, `MainPanelEditorNivelObjetoTest`, `FormularioObjetosObjetoLivreTest` e `CircuitoMetadadosArquivoTest` já passavam sem alteração (testam propriedades herdadas de `ObjetoPista`, não a geometria antiga). `MainPanelEditorDesenhaObjetosCheckboxTest.checkboxDesligado_naoAfetaObjetosDeFuncao` foi ajustado para dar `pontos` (lista, modelo novo) ao `ObjetoEscapada` de teste. `ObjetoConstrucaoTest` (não listado originalmente, mas afetado pelo Grupo 3) teve a constante `GAP_CAMINHAO` atualizada de 10 para 5 em 5 asserções.
- [x] 6.3 Adicionado `MainPanelEditorEscapadaValidacaoTest.java`: cobre clique exato sobre nó do traçado 1, sobre nó do traçado 2, clique longe de qualquer traçado (rejeitado) e saída validada contra o traçado errado (rejeitado) vs. traçado certo (aceito) — via os helpers `noMaisProximoTracado1e2`/`noMaisProximoDoTracado`.
- [x] 6.4 Adicionado `MainPanelEditorViewportCullingTest.java`: objeto dentro do viewport continua desenhado, objeto fora não é desenhado, editor sem `scrollPane` montado não corta nada, e objeto rotacionado cujo raio circunscrito toca o viewport é considerado visível (enquanto a mesma área sem rotação não é). Durante a escrita desse teste foi descoberto e corrigido um bug real: `obterArea()` de `ObjetoArquibancada`/`ObjetoConstrucao` só é populada dentro de `desenha()`, então cortar com base nela antes da primeira chamada os escondia permanentemente; `estaVisivelNoViewport` agora trata área degenerada (`width<=0 || height<=0`) como "sempre visível".
- [x] 6.5 Adicionado `ObjetoLivrePadraoRotacaoTest.java`: `LISTRADO`/`XADREZ` fazem roundtrip em `setTipo`/`getTipo`; padrão listrado com `angulo=0` fica constante ao longo de uma linha horizontal (poucas transições de cor) e com `angulo=90` passa a cruzar várias faixas na mesma linha (prova que o padrão gira com a forma, não só a silhueta); xadrez desenha a cor secundária com e sem rotação; a silhueta em si não muda de bounds com `angulo` (retângulo não quadrado, `angulo=0` vs `90`); e um teste dedicado de cobertura total do padrão nos 4 cantos de um retângulo alongado rotacionado (brita), que falha com o bounding-box antigo e passa com a área expandida ao raio circunscrito.
- [x] 6.6 Coberto pelas asserções já existentes em `ObjetoConstrucaoTest` (item 6.2), atualizadas para o novo vão de 5px.
- [x] 6.8 **(adicionado após feedback do usuário)** Adicionado `MainPanelEditorEscapadaCliqueTest.java`: fluxo real de clique via `MouseEvent` dispatchado no `MouseAdapter` de verdade (não manipulação direta de campos) — entrada validada, ponto livre no meio aceito sem validação alguma, saída validada e finaliza; entrada inválida não adiciona ponto; saída no traçado errado não finaliza. É o teste que expôs e comprova a correção do bug "valida no segundo nó" (ver 4.10).
- [x] 6.9 **(adicionado após feedback do usuário)** Adicionado `MainPanelEditorEscapadaArrastarPontoTest.java` (fluxo de edição via `mousePressed`/`mouseDragged`/`mouseReleased` reais) e `MainPanelEditorEscapadaRevetorizarTest.java` (revetorizar com os mesmos parâmetros não invalida os índices gravados). O primeiro expôs e comprova a correção do bug de resincronização de `posicaoQuina` (ver 4.12) — sem a correção, a posição do ponto arrastado saía deslocada da posição realmente solta pelo usuário.
- [x] 6.7 `mvn test` completo: 478 testes, 0 falhas, 0 erros, 2 skipped (pré-existentes, não relacionados a esta mudança).
- [x] 6.10 **(pedido e depois revertido pelo usuário)** `distancia(Point, Point)`/`distanciaAoSegmento(Point, Point, Point)` foram convertidos para `int` e, em seguida, revertidos de volta para `double` a pedido do usuário — ficam como estavam antes desta mudança. `mvn test` continua em 478/478.
