## 1. Inicialização do editor sem circuito pré-carregado

- [x] 1.1 Alterar `iniciarComNavegacao()` em `MainPanelEditor.java` para nunca chamar `carregarCircuitoExistente(circuitosXml.get(0))` automaticamente — sempre entrar no ramo `indiceCircuito = -1` sem circuito carregado, mesmo com `circuitosXml` não vazia.
- [x] 1.2 Ajustar `repopularComboCircuitos()`/`atualizarBotoesNavegacao()` para suportar `indiceCircuito == -1` com `circuitosXml` não vazia, sem forçar `comboCircuito.setSelectedIndex(0)`, garantindo que o combo box não mostre nenhum circuito pré-selecionado. (já suportado pela lógica existente — `temCircuito` já é `false` quando `indiceCircuito == -1`; nenhuma mudança de código necessária além de 1.1.)
- [x] 1.3 Conferir que selecionar manualmente um circuito no `comboCircuito` (fluxo já existente) continua carregando-o normalmente a partir do estado inicial "nenhum circuito". (confirmado por leitura de código — `ActionListener` do combo já trata `indiceCircuito == -1` corretamente.)
- [x] 1.4 Escrever/ajustar teste (`MainPanelEditorInicializacaoTest.java`) cobrindo: editor não seleciona nenhum circuito no combo após `popularCircuitos()` com lista não vazia; `indiceCircuito` permanece -1.

## 2. Bloqueio de salvamento com escapada incompleta

- [x] 2.1 Adicionar verificação em `salvarPista()` (`MainPanelEditor.java`) que itera `circuito.getObjetos()`, filtra `ObjetoEscapada`, e detecta `getIndiceSaida() == -1`.
- [x] 2.2 Adicionar chave de idioma nova (`escapadaIncompleta`) em `src/main/resources/idiomas/mensagens_XX.properties` para todos os idiomas suportados.
- [x] 2.3 Adicionar método `protected void alertaEscapadaIncompleta()` em `MainPanelEditor.java` (JOptionPane via `Lang.msg(...)`), chamado quando a verificação 2.1 encontrar escapada incompleta, abortando o salvamento antes do `JFileChooser`/`vetorizarCircuito()`.
- [x] 2.4 Adicionar override de `alertaEscapadaIncompleta()` em `MainPanelEditorTestDouble.java` (registra a chamada, sem abrir diálogo real).
- [x] 2.5 Escrever teste (`MainPanelEditorSalvarEscapadaIncompletaTest.java`) cobrindo: salvar com `ObjetoEscapada` de `indiceSaida == -1` é bloqueado e chama o alerta; `existeEscapadaIncompleta()` retorna `false` com todas as escapadas completas ou sem nenhuma `ObjetoEscapada`.

## 3. Bloqueio de salvamento sem distância (quilometragem) informada

- [x] 3.1 Adicionar verificação em `salvarPista()` (`MainPanelEditor.java`), antes do `Integer.parseInt(distanciaKmText.getText().trim())` existente, checando campo vazio ou valor parseado `<= 0` (via novo `parseDistanciaKmOuZero(String)`).
- [x] 3.2 Adicionar chave de idioma nova (`distanciaCircuitoNaoInformada`) em `src/main/resources/idiomas/mensagens_XX.properties` para todos os idiomas suportados.
- [x] 3.3 Adicionar método `protected void alertaDistanciaNaoInformada()` em `MainPanelEditor.java`, chamado quando a verificação 3.1 falhar, abortando o salvamento antes de qualquer parse ou escrita em arquivo.
- [x] 3.4 Adicionar override de `alertaDistanciaNaoInformada()` em `MainPanelEditorTestDouble.java`.
- [x] 3.5 Escrever teste (`MainPanelEditorSalvarDistanciaTest.java`) cobrindo: `parseDistanciaKmOuZero` com vazio/não numérico/válido; salvar com campo vazio é bloqueado sem lançar `NumberFormatException`; salvar com distância `0` é bloqueado.

## 4. Borda branca do traçado do box com interseção suprimida

- [x] 4.1 Em `PainelCircuito.java`, implementar construção de `Shape formaPista` a partir do caminho de nós da pista com o stroke `pista` já usado por `desenhaPista()`, representando a área que a pista pinta (via novo helper `construirCaminhoFechado(List<No>)`, reaproveitado também para o box).
- [x] 4.2 Implementar construção de `Shape formaBordaBox` a partir do caminho de nós do box (`circuito.getBoxKey()`) com um novo `Stroke boxBorda` (largura `.5` de `larguraPistaPixeis`, mais larga que o stroke `box` existente de `.4`), cacheado no mesmo bloco condicional dos outros strokes.
- [x] 4.3 Implementar `Area areaBorda = new Area(formaBordaBox); areaBorda.subtract(new Area(formaPista));`, seguindo o idiom já usado em `ObjetoConstrucao.java` (`Area`/`subtract`).
- [x] 4.4 Criar método `desenhaTintaPistaBox(Graphics2D g2d)` que pinta `areaBorda` em `Color.WHITE`, chamado em `desenhaBackGroundComStrokes()` e `desenhaCircuito()` imediatamente antes de `desenhaPistaBox()` (mesma relação de ordem que `desenhaTintaPistaZebra()` → `desenhaPista()`).
- [x] 4.4b Descoberto durante 4.5 (verificação manual pelo usuário): a tela ao vivo do editor NÃO usa `PainelCircuito` — `MainPanelEditor.paintComponent()` renderiza via `DesenhoProceduralCircuito.desenhaPistaZebraEBox()`/`desenhaPistaBox()`, uma implementação separada. Duplicada a mesma lógica (`construirCaminhoFechado`/`boxBorda`/`Area.subtract`/`desenhaTintaPistaBox`) em `DesenhoProceduralCircuito.java`, chamada em `desenhaPistaZebraEBox()` antes de `desenhaPistaBox()`. Coberto por `DesenhoProceduralCircuitoTest.desenhaPistaZebraEBox_desenhaBordaBrancaAoRedorDoTracadoDoBoxLongeDaPista`.
- [x] 4.4c **Regressão encontrada ao testar manualmente**: com o editor não carregando mais nenhum circuito ao iniciar (seção 1), `MainPanelEditor.paintComponent()` passou a chamar `DesenhoProceduralCircuito.desenhaPista()`/`desenhaTintaPistaEZebra()` com um `Circuito` vazio (`pistaKey` vazio) — ambos faziam `circuito.getPistaKey().get(0)` sem guarda (diferente de `desenhaPistaBox()`, que já tinha essa guarda pro box), causando `IndexOutOfBoundsException` ao abrir o editor. Corrigido com a mesma guarda (`if (getPistaKey() == null || isEmpty()) return;`) nos dois métodos. Coberto por `DesenhoProceduralCircuitoTest.desenhaPistaZebraEBox_comCircuitoVazio_naoLancaExcecao`.
- [x] 4.4d **Bug encontrado ao testar visualmente**: `construirCaminhoFechado()` (em `PainelCircuito` e `DesenhoProceduralCircuito`) fechava o caminho do box de volta ao seu PRIMEIRO nó, tratando-o como um loop — igual à pista. Mas o box é um trajeto aberto (entrada → saída, sem voltar), como o próprio `desenhaPistaBox()` original já deixava claro (a "linha de fechamento" ali é um no-op, do último nó pra ele mesmo). Isso desenhava uma faixa branca larga e espúria entre o primeiro e o último nó do box. Corrigido: renomeado para `construirCaminho(nos, fechado)` com um parâmetro booleano — `true` (fecha o loop) continua usado só pra pista, `false` (caminho aberto) passa a ser usado pro box. Coberto por `DesenhoProceduralCircuitoTest.desenhaPistaZebraEBox_naoFechaOTracadoDoBoxDeVoltaAoPrimeiroNo`.
- [x] 4.5 Confirmado o algoritmo em dois níveis: (a) script standalone reproduzindo `GeneralPath`/`BasicStroke.createStrokedShape`/`Area.subtract`, renderizado em `BufferedImage` e inspecionado por pixel — confirma que a borda branca aparece no trecho livre e desaparece no sobreposto; (b) teste automatizado real (`DesenhoProceduralCircuitoTest`) contra o método efetivamente usado pela tela do editor (ver 4.4b), confirmando pixel branco no trecho livre do box. Confirmado interativamente pelo usuário rodando o editor real (que também revelou a regressão 4.4c, já corrigida).
- [x] 4.6 Avaliado: geometria recalculada a cada frame (sem cache), no mesmo padrão já usado por `desenhaPista()`/`desenhaPistaBox()` (que também recomputam as coordenadas por nó a cada chamada) — só os `Stroke`s são cacheados, como antes desta mudança. Sem cache adicional por ora; se testes de performance no jogo real revelarem gargalo, cachear `areaBorda` invalidando por `larguraPistaPixeisLoc`/revetorização fica como próximo passo.

## 5. Listas de nós da pista/box redimensionáveis (JSplitPane)

- [x] 5.1 Em `MainPanelEditor.gerarListsNosPistaBox()`, trocar o `JPanel controlPanel` com `GridLayout(2, 1)` por `JSplitPane splitNos = new JSplitPane(JSplitPane.VERTICAL_SPLIT, pistas, boxes)`, com `setResizeWeight(0.5)` e `setOneTouchExpandable(true)`, seguindo literalmente o padrão de `gerarSecaoObjetos()`/`splitListas` — incluindo comentário explicando a proporção escolhida (50/50, conteúdo simétrico, diferente do 70/30 do caso `listaDesenho`/`listaFuncao`).
- [x] 5.2 Remover os overrides de `getPreferredSize()` que fixavam altura 160 em `pistaJListJScrollPane` e `boxJListJScrollPane` (e o import agora não usado de `java.beans.Transient`), já que o `JSplitPane` passa a controlar a altura de cada lado.
- [x] 5.3 Conferido por leitura + teste (`MainPanelEditorListsNosPistaBoxTest.java`): `gerarSecaoNos()` continua funcionando (`gerarPainelControlesNos()` no `NORTH`, `gerarListsNosPistaBox()` no `CENTER`), sem alteração necessária.
- [x] 5.4 Verificado automaticamente via `MainPanelEditorListsNosPistaBoxTest.java` (orientação vertical, `resizeWeight=0.5`, `oneTouchExpandable=true`, as duas `JList` continuam presentes). Arrastar o divisor interativamente não foi testado manualmente na UI real neste ambiente — comportamento padrão do `JSplitPane` (já usado em `gerarSecaoObjetos()`), sem lógica customizada adicional.

## 6. Validação final

- [x] 6.1 Rodar `mvn test` e garantir que a suíte completa passa, incluindo os novos testes das seções 1–3.
- [x] 6.2 Rodado o editor manualmente pelo usuário (verificação interativa dos cinco comportamentos). Dois bugs encontrados e corrigidos:
  - **Escapada incompleta não bloqueava o salvamento**: `existeEscapadaIncompleta()` só verificava `circuito.getObjetos()` — uma `ObjetoEscapada` com a entrada já clicada mas ainda sem o clique direito de finalização nunca chega a essa lista (só é adicionada ao finalizar), então salvar (F8) nesse meio-tempo não era bloqueado e descartava o desenho em andamento silenciosamente. Corrigido em `MainPanelEditor.existeEscapadaIncompleta()`: também conta como incompleta quando `desenhandoObjetoLivre && objetoPista instanceof ObjetoEscapada`. Cobrido por `MainPanelEditorSalvarEscapadaIncompletaTest.salvar_comEscapadaSendoDesenhadaAindaNaoFinalizada_ehBloqueadoEChamaAlerta`/`existeEscapadaIncompleta_comEscapadaSendoDesenhada_retornaTrue`.
  - **Borda branca do box 2x mais larga que a da pista principal**: `boxBorda` usava margem de `.5 - .4 = .1` (em unidades de `larguraPistaPixeis`) sobre o stroke central do box (`.4`), o dobro da margem análoga que `zebra` (`1.05`) usa sobre `pista` (`1.0`, margem `.05`) na pista principal. Corrigido para `.45` (margem `.05`, igualando a proporção da pista) em `PainelCircuito.java` (dois pontos: `desenhaBackGroundComStrokes()` e `desenhaCircuito()`) e em `DesenhoProceduralCircuito.desenhaPistaZebraEBox()` — mantendo as duas implementações consistentes entre si. Ajustado o pixel de amostra em `DesenhoProceduralCircuitoTest.desenhaPistaZebraEBox_desenhaBordaBrancaAoRedorDoTracadoDoBoxLongeDaPista` (a margem mais estreita move a faixa branca testável para mais perto da linha central).
