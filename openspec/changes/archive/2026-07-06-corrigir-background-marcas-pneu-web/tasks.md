## 1. Backend: geração independente de marca-só vs. marca+fumaça

- [x] 1.1 Em `ControleJogoLocal.geraTravadaRoda` (src/main/java/br/f1mane/controles/ControleJogoLocal.java:1039-1055), separar a decisão de fumaça da decisão de marca: manter `piloto.setMarcaPneu(true)` incondicional, mas só chamar `piloto.setTravouRodas(qtdeFumaca)` com `qtdeFumaca > 0` numa fração dos casos; nos demais, chamar `piloto.setTravouRodas(0)`.
- [x] 1.2 Definir o critério/probabilidade de gerar fumaça (ex.: reaproveitar o mesmo `getRandom()` já usado para `qtdeFumaca`, com uma constante `Global` nova ou limiar fixo — documentar a escolha no código).
- [x] 1.3 Adicionar testes unitários em `ControleJogoLocalTest`/classe equivalente cobrindo: travada de roda sem fumaça mantém `isMarcaPneu()==true` e `isTravouRodas()==false`; travada com fumaça mantém ambos `true`; distribuição ao longo de várias chamadas não é 100% fumaça.
- [x] 1.4 Rodar `mvn test` e confirmar que os testes existentes de `zona-frenagem-marcas-intensidade`/consolidação de stress continuam passando sem alteração.

## 2. Backend: expor marcas de pneu por nó no payload de posição

- [x] 2.1 Levantar a estrutura atual de `Posis`/`PosisPack` e `ControleJogosServer.gerarPosicaoPilotos` (src/main/java/br/f1mane/servidor/**/ControleJogosServer.java:412-452) para adicionar um campo de lista de `TravadaRoda` (idNo+tracado) geradas no tick.
- [x] 2.2 Popular essa lista a partir dos eventos gerados por `ControleJogoLocal.geraTravadaRoda` (sem afetar o comportamento já existente do caminho Swing/`GerenciadorVisual`). Nota de design: em vez de um delta "só deste tick" (que um `drain` esvaziaria antes do segundo cliente pollar), o backend mantém um mapa deduplicado por `idNo_tracado` (`ControleRecursos.marcasPneuGeradas`, limpo só ao carregar novo circuito) e envia a lista completa a cada poll — ver nota em `design.md`.
- [x] 2.3 Codificar a lista usando `TravadaRoda.encode()` (já existente) para manter o payload compacto, e ajustar o decode correspondente do lado do cliente.
- [x] 2.4 Confirmar que o campo é aditivo — clientes que ainda não leem o novo campo continuam funcionando com o comportamento atual (status `"T"`/`"M"` por piloto).
- [x] 2.5 Adicionar/ajustar teste no endpoint (`LetsRace`/`ControleJogosServer`) confirmando que uma travada de roda gerada aparece no snapshot do payload em qualquer poll subsequente (não só no primeiro), e que múltiplos polls seguidos continuam vendo a mesma marca (`ControleJogosServerTravadaRodaPayloadTest`).

## 3. Cliente web: consumir marcas de pneu por nó em vez de inferir por status

- [x] 3.1 Em `cpu.js` (`cpu_dadosParciais`/`cpu_processaMarcasPneu`), parsear a nova lista `posisPack.travadaRodas` do payload JSON e acumular num `Map` local (`marcasPneuAcumuladas`, chaveado por `idNo+"_"+tracado`) que só é limpo na chuva (mesma regra de `vdp_desenhaBorrachaNaPista`).
- [x] 3.2 Em `vdp.js`, substituir `vdp_desenhaTravadaRoda` (disparado por piloto por frame) por `vdp_registraMarcaPneuNo(idNo, tracado)`, chamado uma vez por marca nova a partir de `cpu_processaMarcasPneu`; calcula o ponto/ângulo do nó via `vdp_pontoTracado`/`gu_calculaAngulo` (mesmo padrão de `eixoCarro` usado na renderização do carro) e empilha em `borrachaNaPista`.
- [x] 3.3 Mantido: `pilotosTravadaFumacaMap`/`vdp_desenhaTravadaRodaFumaca` continuam controlados exclusivamente pelo status `"T"` por piloto; removida a linha `pilotosTravadaMap.set(...)` do branch `"T"` e todo o branch `"M"` (que não tinha mais nada a fazer client-side).
- [x] 3.4 `marcasPneuAcumuladas` garante que um `idNo+tracado` já visto não é reprocessado; `marcasPneuAcumuladas.clear()` foi adicionado junto ao reset de `borrachaNaPista` na chuva, pra não perder marcas já conhecidas quando a chuva passar.

## 4. Cliente web: preload do background no clique de "Jogar"

- [x] 4.1 Em `jogar.js`, adicionada `jogar_preCarregaBackGround(nomeCircuito)`, chamada no `success` de `jogar()` e `jogarCampeonato()` (as duas formas de confirmar "Jogar"): busca `circuito.backGround` via `/rest/letsRace/circuito?nomeCircuito=...` e dispara `new Image().src = "/flmane/rest/letsRace/circuitoJpg/" + backGround` imediatamente, em paralelo com `window.location.href = "corrida.html"`.
- [x] 4.2 Como `jogar.html`→`corrida.html` é uma navegação de página cheia (não SPA), a única forma de a requisição "sobreviver" é o cache HTTP do navegador servir a mesma URL de novo sem round-trip ao servidor — por isso `LetsRace.circuitoJpg` (`src/main/java/br/f1mane/servidor/rest/LetsRace.java`) passou a responder com `Cache-Control: max-age=120`, já que antes não tinha nenhum header de cache e o navegador não tinha base pra reaproveitar a resposta.
- [x] 4.3 `mid_carregaBackGroundCorrida`/`imgBgSolicitado` (`mid.js`) não foram alterados — o guard existente já evita uma segunda chamada de `.src` dentro da própria `corrida.html`; a requisição de rede em si (não o disparo em JS) é que passa a ser evitada via cache HTTP quando o preload de `jogar.js` já rodou.
- [x] 4.4 Confirmado: o handler do carrossel (`$('#circuitoCarousel').on('slide.bs.carousel', ...)`, linha 82-84) só atualiza `circuitoSelecionado`, sem chamar `jogar_preCarregaBackGround` — só o clique em "Jogar"/"Jogar Campeonato" dispara o preload.

## 5. Validação manual (QA visual)

- [x] 5.1 Rodado localmente (`mvn package -Ph2` + `java -jar target/flmane.jar --headless`, preview em `http://localhost:8080`). Confirmado via network log: `GET .../rest/letsRace/circuitoJpg/interlagos_mro.jpg` dispara logo após `GET .../rest/letsRace/jogar/...` (no clique de "Jogar"), bem antes de `corrida.html` carregar — e não mais só quando o estado chega a qualify/corrida. Um bug real foi pego e corrigido nessa verificação: a navegação (`window.location.href`) estava acontecendo antes do preload assíncrono (`/circuito?nomeCircuito=`) completar, derrubando o contexto JS de `jogar.html` e cancelando o preload; corrigido fazendo a navegação esperar o callback do preload (com timeout de segurança de 1.5s).
- [x] 5.2 Corrida rodada do início ao fim sem erros de console; `travadaRodas` chega no payload de `dadosParciais` e `cpu_processaMarcasPneu`/`vdp_registraMarcaPneuNo` processam sem exceções. A observação visual direta de uma marca isolada (sem fumaça) não foi capturada nesta sessão — é um evento probabilístico (chance de frenagem forte na zona de frenagem) e a corrida rodada terminou antes de um lock-up ficar visível na janela de observação; a independência marca/fumaça está coberta por teste unitário determinístico (`ControleJogoLocalGeraTravadaRodaFumacaTest`).
- [x] 5.3 Rendering da pista (fundo, carros, HUD) confirmado íntegro via screenshot durante a corrida — sem regressão visual dos efeitos existentes. O reset de `marcasPneuAcumuladas` junto com `borrachaNaPista` na chuva foi implementado (`vdp_desenhaBorrachaNaPista`) mas não observado ao vivo (a corrida rodada teve céu limpo); comportamento equivalente ao pré-existente (`borrachaNaPista = []`), só estendido para o novo conjunto de dedupe.

## 6. Resgatar a tela de espera (contador + msgCarregando) na sala de espera

- [x] 6.1 Diagnosticado via `git show e2cbcb73~1`: antes do `e2cbcb73`, `mid_caregaMidia()` setava `imgBg.src` imediatamente, então na sala de espera (estado 07) o `cvBg` era criado assim que a imagem baixava, `vdp_desenha` passava do gate `!cvBg` e `vdp_ctl()` exibia o overlay de `ctl_desenhaInfoSegundosParaIniciar` (contador `iniciaEm : segundosParaIniciar` + `msgCarregando`). Depois do `e2cbcb73`, o estado 07 não solicitava o background, `cvBg` nunca existia e a tela de espera inteira ficava em branco durante toda a contagem.
- [x] 6.2 Em `cpu.js` (`cpu_main`), chamar `mid_carregaBackGroundCorrida()` em todos os estados, inclusive o 07 — restaurando a tela de espera; atualizado o comentário correspondente em `mid.js`. A configuração do contador continua sendo `Global.SEGUNDOS_PARA_INICIAR_CORRRIDA` (60s, `br/nnpe/Global.java`), enviada ao cliente como `dadosJogo.segundosParaIniciar` (`ControleJogosServer.java:334`), e as mensagens `iniciaEm`/`msgCarregando` continuam em `src/main/resources/idiomas/mensagens_XX.properties` — nada disso tinha sido removido, só ficou inalcançável pelo gate `!cvBg`.
- [x] 6.3 QA visual: sala de espera exibe a pista (centrada na largada), "Inicia em : N" decrementando e "Corrida começará em alguns instantes"; ao zerar, a transição para a largada (estado 13, grid + semáforo) acontece normalmente sem recarregar a imagem.
